# vim: set autoindent smartindent ts=4 sw=4 sts=4 noet filetype=sh:

if [[ -x "$HOME/bin/cmake/bin/cmake" ]]; then
	if [[ ":$PATH:" != *":$HOME/bin/cmake/bin:"* ]]; then
		PATH="$HOME/bin/cmake/bin:$PATH"
	fi
fi
